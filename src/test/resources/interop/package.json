{
  "name": "js-sdk-interop",
  "private": true,
  "type": "module",
  "description": "Mints a token with the published TypeScript SDK so the Java suite can verify it.",
  "dependencies": {
    "@unicitylabs/state-transition-sdk": "3.0.0"
  }
}
